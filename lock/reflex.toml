reflex = 1

description = """
Lock the screen.
Everything keeps running behind it."""

not_for = ["sleeping, restarting or shutting down", "logging out", "turning the display off"]
tags    = ["screen"]
effect  = "write"
confirm = "Lock the screen?"
run     = "lock.mts"

[needs]
runs = ["open"]

[examples]
"lock the screen"            = {}
"lock my laptop"             = {}
"I'm stepping away, lock it" = {}

[tests]
"lock"                    = {}
"secure the screen"       = {}
"log me out"              = false
"put the laptop to sleep" = false
